# KENT-438 CI failure round

- [x] Identify the exact failing CI test and determine whether it is branch-caused, merge-caused, or external/flaky.
- [x] Reproduce the failure locally with a focused test.
- [x] Fix the root cause with focused regression coverage, if a code correction is warranted.
  - Root cause: the workflow-view test runner released the workflow execution lease before returning the Authority handle. This let Authority publish Exact Running before `CurrentNodeController` validated and released the admission gate, bypassing the production ownership order.
- [x] Run focused verification and the required build.
  - Exact failed test: 100 repetitions passed.
  - Exact failed test under race: 50 repetitions passed.
  - Full `server/workflowview` suite passed.
  - Broader race matrix passed repeated iterations but hit the harness's 300-second wall-clock cap before completing all 10 requested repetitions; no test or race failure was reported.
  - Required build passed.
- [x] Adjudicate the two review threads that arrived while the CI failure was being resolved.
  - Consolidate Attention ask-ID equality and duplicate validation through `runtimeids.EqualSets`.
  - Represent absent lifecycle SQL projection state as explicit `NULL`; reject malformed zero-valued present state.
- [x] Re-run affected verification after review remediation.
  - Full `shared/serverapi`, `server/metadata/querygen`, `server/metadata/sqlitegen`, and `server/workflowview` suites passed.
  - Focused `server/workflowstore` lifecycle suite passed.
  - Full `server/workflowstore` exposed unrelated pre-existing manual-move filesystem timing flakes; both exact tests also reproduced intermittently without touching the changed lifecycle projection path.
  - Generated-query freshness checks and required build passed.
- [x] Commit and push the complete correction atomically.
- [x] Merge the newly advanced `origin/main` and adjudicate integration conflicts.
  - Retained both KENT-438 board lifecycle coverage and main's bounded Session-label test.
  - Supplied the lifecycle Store required by KENT-438's static capture source.
  - Adapted retained-activation acceptance tests to main's richer `LiveSessions` API without dropping KENT-438 coverage.
  - Full `server/workflowview`, focused retained-activation CLI, architecture guard, and required build passed.
- [x] Push the current-main merge.
- [x] Resolve the post-merge lifecycle generator review finding.
  - One typed Question-field descriptor now generates declarations, identity/full arguments, and scan targets.
  - Generator freshness tests and required build passed.
- [x] Push the final review correction and confirm PR readiness.
  - PR head `20892924f` is mergeable against current base `89dfe4486`.
  - No unresolved review threads remain.
  - Fresh CI is running.
- [ ] Return PR #724 to the watcher.
- [ ] Confirm PR readiness and unresolved review-thread state.
- [ ] Return PR #724 to the watcher.
