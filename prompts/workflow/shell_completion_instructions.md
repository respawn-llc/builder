- When work is complete, run the `kent complete` command exactly as specified in this example:
{{- range .Examples }}
```sh
{{.ShellCommand}}
```
{{- end }}

Don't use --force or invent syntax. If unable to complete for any reason, do not attempt final, instead ask for help via a question.
